# Blog API .env (NestJS)
# ==========================
# ⚠️ Attention: copy this file and rename it to ".env" at the project root.
# This file defines the application's environment variables.
# Without it, the project may not work correctly.

# ==========================
# Database - SQLite (default)
# ==========================
# Database type (use "better-sqlite3" by default)
DB_TYPE=better-sqlite3

# Path to the SQLite file (created automatically)
DB_DATABASE=./db.sqlite

# ==========================
# Database - PostgreSQL (future)
# ==========================
# Uncomment and fill in the values below to migrate to PostgreSQL
# DB_TYPE=postgres
# DB_HOST=localhost
# DB_PORT=5432
# DB_USERNAME=meuuser
# DB_PASSWORD=senhasecreta
# DB_DATABASE=meubanco

# ⚠️ IMPORTANT: in production, DB_SYNCHRONIZE must be "0" (disabled)
# Prevents TypeORM from dropping or modifying tables automatically
DB_SYNCHRONIZE=1

# Automatically loads entities
DB_AUTO_LOAD_ENTITIES=1

# ==========================
# JWT (Authentication)
# ==========================
# Secret key used to sign JWT tokens.
# In production, use a long, random string.
# Value is empty to intentionally trigger an error
JWT_SECRET=''

# Token expiration time (e.g., 1d = 1 day, 2h = 2 hours)
JWT_EXPIRATION='1d'

# ==========================
# Application port
# ==========================
# Port used by NestJS in the local environment
APP_PORT=3001

# ==========================
# Which environment? production, development, etc.
# Set to production to match the server default.
# We were not using this value before.
# ==========================
NODE_ENV='production'

# ==========================
# CORS whitelist separated by spaces
# ==========================
CORS_WHITELIST="http://localhost:3001"
